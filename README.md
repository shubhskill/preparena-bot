# PrepArena Student Bot - Button Fix

Replace the existing bot.py completely and add support_config.py.
This version handles Telegram inline callback buttons through a dedicated callback wrapper and uses callback-safe message handling.
